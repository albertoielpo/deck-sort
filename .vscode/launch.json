{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Yaml sort",
            "program": "${workspaceRoot}/target/debug/deck-sort",
            "console": "integratedTerminal",
            "args": ["${workspaceRoot}/example/kong.yaml", "true"],
            "cwd": "${workspaceRoot}/target/debug/",
            "sourceLanguages": ["rust"]
        }
    ]
}